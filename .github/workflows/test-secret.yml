name: Test Secret
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - env:
          T: ${{ secrets.TEST_SECRET }}
        run: |
          echo "len=${#T}"
          if [ "$T" = "hello-world-test" ]; then echo MATCH; else echo NOMATCH; fi
